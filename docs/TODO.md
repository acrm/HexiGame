# TODO

## Recently Completed (2026-04-07)

- [x] Из Settings удалено действие «Сброс сессии»
- [x] В секции Hexipedia «Сессия» playback переведён в одну строку
- [x] Добавлен отдельный session-widget (по умолчанию выключен) с той же playback-строкой
- [x] Кнопка скачивания сессии перенесена из заголовка секции Session на панель содержимого
- [x] Панель Session расширена: детальная статистика + прокручиваемый лог действий от последнего к первому

- [x] Генератор названий сессий упрощён до строгого формата «цвет + цветок» (без минералов/камней), для RU добавлено согласование цвета с родом цветка
- [x] На экране HexiOS в шапке добавлена кнопка Play слева от Settings для запуска самой последней сессии
- [x] Кнопка «Продолжить» в карточках сессий переведена на иконку воспроизведения
- [x] Удаление сессии переведено на локальное подтверждение в строке действий: кнопка удаления переключается в «Отмена», подтверждение выполняется соседней кнопкой

- [x] Стартовый экран переработан под стиль HexiPedia: тёмный секционный layout, под-экран истории сессий, заголовок-вкладка `HexiOS v<marketing>`
- [x] Из стартового экрана удалено поле поиска; вместо него добавлен отдельный ряд выбора языка
- [x] Настройки на стартовом экране перенесены в шестерёнку в правом верхнем углу (единая модель с in-game UI)
- [x] Стартовое меню обновлено: «Подключиться» (если есть активная сессия), «Новая сессия», «История сессий»; панель названа «Сессии»
- [x] Внутриигровые настройки обновлены: вместо «Сбросить сессию» показывается «Отключиться» (без подтверждения), возвращающая на стартовый экран с сохранением состояния
- [x] Добавлено per-session хранилище снимков `hexigame.session.byId.<sessionId>` для переключения между записями истории через «Продолжить»
- [x] Аудио lifecycle унифицирован: музыка и UI-клики непрерывно и единообразно работают на стартовом экране и в игре

## Recently Completed (2026-03-22)

- [x] Стартовый экран обновлён: выбор языка (EN/RU) с мгновенным переключением без перезагрузки страницы
- [x] Session-aware startup: кнопки «Продолжить» + «Начать заново» при наличии сохранённой сессии; только «Новая игра» если сессии нет
- [x] Реактивный i18n: `subscribeToLanguageChange()` рассылает обновления всем подписчикам; Settings и GuestStart переключают язык без `window.location.reload()`
- [x] `resetSession` теперь создаёт новый RNG через `createRng()` — каждая новая игра имеет уникальный мир
- [x] Метаданные платформ обновлены: itch.io и Яндекс.Игры — мобильные управление, языки, session UX

## Recently Completed (2026-04-03)

- [x] Переименование мобильных вкладок и UI-терминов: `HexiMap` -> `Map`, `HexiLab` -> `Lab`, `HexiPedia` -> `Hexipedia` (включая имена файлов/компонентов)
- [x] Добавлена совместимость legacy tab-id (`heximap`/`hexilab`) при загрузке сохранений с миграцией в новые значения (`map`/`lab`)
- [x] Кнопка Disconnect в игровом tab bar приведена к визуалу Settings (белая, тот же размер, тот же фон)
- [x] Исправлен overlay-слой Settings: окно настроек корректно отображается поверх экрана выбора сессий
- [x] Экран выбора сессий переработан: фиксированный header, отдельная прокрутка language panel и отдельная прокрутка sessions panel
- [x] Панель сессий сделана сворачиваемой до заголовка (по умолчанию развернута)
- [x] Список сессий унифицирован: без разделения на текущую/прочие, сортировка по последней активности, продолжить можно любую
- [x] Каждая сессия в списке по умолчанию свёрнута в однострочный формат (экспандер, имя, дата последнего действия, кнопка старта справа)
- [x] Кнопка «Новая сессия» на стартовом экране теперь только добавляет новую запись (с подготовленным snapshot/log) и не запускает её автоматически
- [x] Для каждой сессии в карточке отображаются все ключевые метрики; при отсутствии данных показывается `—`
- [x] Добавлены автокодовые имена сессий (цвет + цветок) на текущем языке интерфейса с проверкой уникальности и нумерацией коллизий
- [x] Добавлено ручное переименование сессий и точечное удаление с инлайн-подтверждением без системных диалогов
- [x] Стартовый экран приведён к ретро-ограничениям: единый моношрифт, потолок кегля `16px`, ellipsis при переполнении, 8-битная палитра, текстовые символы вместо FA-иконок
- [x] Стартовый экран переведён в полноценный TUI-стиль: layout по строкам/столбцам, символьные рамки панелей/кнопок (box-drawing), единый кегль `16px`

- [x] Стартовый экран приведён к стилю игрового UI: единая палитра, шрифты и кнопки, внутри того же mobile portrait-контейнера
- [x] Меню стартового экрана упрощено до действий: «Продолжить сеанс» (если есть), «Новый сеанс», «Настройки», «Скачать сеанс» (если есть)
- [x] Добавлен экспорт сохранённой сессии в JSON-файл из стартового меню
- [x] Мобильная раскладка исправлена: фиксированная ширина игрового контейнера, адаптация по высоте устройства, контролы выведены из наложения на поле; при нехватке высоты игровое поле пропорционально ужимается

## Recently Completed (2026-03-21)

- [x] Паттерн Инь-Ян загружен по умолчанию в `/editor` с кнопкой Clear
- [x] Переделана палитра: крупные кнопки цветов с процентами внутри, выбор базового цвета вынесен в ряд радиокнопок под палитрой
- [x] Переделан `/editor` на упрощённую модель: одно текстовое поле для координат и визуальный выбор текущего цвета рисования
- [x] JSON в `/editor` теперь группируется по `r`, сортируется по `q` внутри группы, одна координата на строку
- [x] Синхронизация в обе стороны: клик по сетке обновляет JSON, редактирование JSON обновляет сетку
- [x] В `/editor` добавлена поддержка `relativeColor` с выбором базового цвета палитры (0%) и пересчётом цветов по палитре
- [x] В `/editor` добавлена поддержка формата `{ anchorCell, cells }` и режим постановки якоря отдельной кнопкой
- [x] Клетка-якорь в `/editor` отрисовывается чёрным кольцом вокруг координат, поддерживается только один якорь
- [x] Вспомогательная страница `/editor` для ручного редактирования аксиальных паттернов
- [x] Task flow переделан вокруг optional заданий: виджет живёт в фазах `pending/active/complete`, модалка открывается только из `pending`, а setup задания применяется только после «Начать»
- [x] Детерминированная 5-шаговая цепочка заданий в непрерывном мире без полного reset между шагами: исследование, сбор 4 отмеченных гексов за границей видимости, раскопка двух колец, сбор набора 3+3 для любой противоположной пары цветов, финальный Инь-Янь; радужная задача убрана из активной цепочки
- [x] HexiPedia и overlay-виджеты переделаны под модель Tasks/Stats/Structures/Colors: добавлен structures widget и session-scoped список начатых структур
- [x] Цветные гексы сделаны непроходимыми для черепашки с поиском достижимого маршрута до цели
- [x] Недоступная цель в мире подсвечивается красной рамкой и сопровождается звуковым сигналом
- [x] Механизм подсветки точек интереса за границей видимости: используется линия pathfinding от черепашки к скрытой цели (с игнорированием препятствий), выбирается последняя видимая клетка пути и подсвечиваются только её угловые точки, совпадающие с реально отрисованной границей точечного поля; клики/тачи вне видимого dotted-контура игнорируются
- [x] Touch UX на HexiMap исправлен: при удержании пальца обновляется только курсор/фокус, а движение черепашки стартует только по отпусканию (`touchend`) по финальной выбранной клетке
- [x] Жизненный цикл сессий исправлен: новая сессия стартует только через «Начать как гость», перезагрузка страницы продолжает ту же сессию из последнего сохранения, а завершение происходит только через «Сбросить сессию»
- [x] Базовый мир обновлён: стартовая анимация ускорена в 3 раза, плотность цветных гексов снижена в 3 раза, видимая зона смещена на 1 клетку вперёд, старт гарантирует проходимый коридор до дальней видимой точки
- [x] Рендер шаблона структуры ограничен видимой областью поля; для активной структуры добавлена закадровая подсветка уже заполненных клеток на границе экрана

## Gameplay Backlog

- [ ] Раздельный деплой стабильной версии и текущей, с возможностью просматривать все предыдущие публичные версии
- [ ] Иерархичный редактор гексагоновых структур
- [ ] Недостающие гексы в инвентаре
- [ ] Нестабильные гексы в мире
- [ ] Бесконечный мир
- [ ] Черепашка-антагонист
- [ ] Стихийные препятствия
- [ ] Таймер уровня
- [ ] Стартовая страница

## Architecture Backlog (2026-03-07)

- [x] Вынести `useGameSession` из `ui/hooks` в `appLogic/sessionController` + `sessionRepository`
- [x] Убрать дублирование `integration.init()` / `onGameReady()` (единая точка orchestration)
- [x] Разделить `GameField.tsx` на renderer/input/viewport модули
- [x] Вынести task flow orchestration из `Game.tsx` в `appLogic/taskFlow` с compatibility-wrapper в `appLogic/tutorialFlow`
- [x] Перевести audio lifecycle на appLogic-порт (audioController + audioDriver)

## Decomposition Candidates (2026-03-07 audit)

- [x] Вынести аудио lifecycle/effects из `src/ui/components/Game.tsx` в отдельный `ui/hooks/useGameAudio.ts`
- [x] Вынести canvas render-loop из `src/ui/components/GameField/GameField.tsx` в `ui/components/GameField/useCanvasRenderer.ts`
- [x] Разделить JSX-слой `src/ui/components/Game.tsx` на контейнеры (`GameMobileTabs`, `GameOverlays`, `GamePanels`) без изменения UX