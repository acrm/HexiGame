// Imported only to provide a stable, no-op file that can be touched from VS Code tasks
// to trigger the same Vite reload cycle as a normal source edit.
export const DEV_RELOAD_MARKER = 'hexigame-dev-reload-marker';